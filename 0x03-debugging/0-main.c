#include "main.h"
/** more headers goes there 
*
* 
*/
int main(void)
{
	int i;

        i = 98;
        positive_or_negative(i);

        if(i == 0){
            printf('%d is zero\n', i);
        }

        return (0);
}